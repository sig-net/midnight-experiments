{
  "name": "@midnight-experiments/break-integration-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Live test for the break experiment: deploys the break contract against the local Midnight stack and calls its circuits under real proving. Gated by RUN_INTEGRATION_TESTS; requires `yarn compile:zk` output and a running node + indexer + proof server.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "test:integration": "RUN_INTEGRATION_TESTS=1 vitest run tests/ --bail 1 --disable-console-intercept"
  },
  "dependencies": {
    "@midnight-experiments/break-contract": "*",
    "@midnight-experiments/test-harness": "*"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
